FROM php:8.1-apache
# Installa estensioni PDO MySQL, cURL, ecc.
RUN docker-php-ext-install pdo_mysql && a2enmod rewrite
# Copia il tuo API
COPY src/ /var/www/html/
# Espone la porta 8080
EXPOSE 8080
# Comando di avvio (Apache già preconfigurato)
CMD ["apache2-foreground"]
